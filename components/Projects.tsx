import { Title } from "./Title";

export const Projects = () => {
  return (
    <div
      id="projects"
      className={`
      bg-white
      `}
    >
      <div className="w-full max-w-6xl mx-auto px-4 py-6">
        <Title text="Projects" />
      </div>
    </div>
  );
};
